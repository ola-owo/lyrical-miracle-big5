[project]
name = "lyrical-miracle-big5"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "datasets>=2.19.1",
    "litserve>=0.2.17",
    "peft>=0.19.1",
    "polars>=1.41.2",
    "sentence-transformers>=5.5.1",
    "torch>=2.12.0",
    "wandb>=0.27.2",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]

[tool.uv.sources]
torch = { index = "pytorch" }

[[tool.uv.index]]
name = "pytorch"
url = "https://download.pytorch.org/whl/cpu"
