[project]
name = "lyrical-miracle-big5"
version = "0.8.0"
requires-python = ">=3.13"
dependencies = [
    "peft>=0.19.1",
    "sentence-transformers>=5.6.0",
    "transformers<5.13.0", # adapter loading broken with 5.13.0 and peft 0.19
    "litserve>=0.2.17",
    "pydantic>=2.13.4",
]

[project.optional-dependencies]
cpu = ["torch>=2.12.0"]
cuda = ["torch>=2.12.0"]

[dependency-groups]
dev = [
    { include-group = "train" },
    { include-group = "test" },
    { include-group = "ai" },
]
train = [
    "marimo>=0.23.9",
    "polars>=1.41.2",
    "wandb>=0.27.2",
    "datasets>=2.19.1",
    "ipython>=9.15.0",
]
test = [
    "fastapi>=0.136.3",
    "httpx2>=2.3.0",
    "pytest>=9.0.3",
]
ai = [
    "mcp>=1.28.1",
    "pydantic-ai>=2.5.0",
]

[project.scripts]
serve = "big5.serve:main"

[tool.uv]
conflicts = [
  [
    { extra = "cpu" },
    { extra = "cuda" },
  ],
]

[tool.uv.sources]
torch = [
  { index = "pytorch-cpu", extra = "cpu" },
  { index = "pytorch-cuda", extra = "cuda" },
]

[[tool.uv.index]]
name = "pytorch-cpu"
url = "https://download.pytorch.org/whl/cpu"
explicit = true

[[tool.uv.index]]
name = "pytorch-cuda"
url = "https://download.pytorch.org/whl/cu132"
explicit = true

[tool.uv.build-backend]
module-name = "big5"
module-root = ""

[build-system]
requires = ["uv_build>=0.11,<0.12"]
build-backend = "uv_build"

[tool.ruff.format]
quote-style = "single"
